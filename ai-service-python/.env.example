# OpenAI API Key (required for LLM-powered answers)
# Without this key, the system falls back to deterministic search-based answers.
OPENAI_API_KEY=sk-...your-key-here...

# FastAPI service port (default: 8000)
PORT=8000
